using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;


public class Fade : MonoBehaviour
{
   /* void Update(){
        Sprite tmpColor = GetComponent<Image>().sprite;

        if(tmpColor.a > 0.0f){

        	Color newColor = new Color(tmpColor.r, tmpColor.g, tmpColor.b, tmpColor.a - Time.deltaTime);
        	tmpColor.color = newColor;
        	GetComponent<Image>().sprite = tmpColor;
        }else{
        	Destroy(gameObject);
        }
    } */
}
